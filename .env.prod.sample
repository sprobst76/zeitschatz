# ZeitSchatz Production Environment
# Copy to .env.prod and fill in real values

# Domain (used by docker-compose.prod.yml and Traefik)
DOMAIN=example.de

# Secret Key - IMPORTANT: Generate a secure random string!
# openssl rand -hex 32
SECRET_KEY=generate-a-secure-random-string

# CORS Origins (the frontend URL)
CORS_ORIGINS=https://zeitschatz.example.de

# Photo settings
PHOTO_RETENTION_DAYS=14

# TAN settings
TAN_DEFAULT_DURATION_MINUTES=30

# FCM (optional, for push notifications)
FCM_SERVER_KEY=
