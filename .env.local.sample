# Lokale Konfiguration (nicht ins Repo committen!)
# Kopiere diese Datei nach .env.local und passe die Werte an

# Deploy Script Konfiguration
DEPLOY_API_URL=https://zeitschatz-api.your-domain.de
DEPLOY_GITHUB_REPO=username/zeitschatz
DEPLOY_N8N_WEBHOOK=http://your-n8n-server:5678/webhook/apk-release

# Server SSH Zugang (fuer rsync/deploy)
DEPLOY_SERVER_USER=user
DEPLOY_SERVER_HOST=your-server-ip
DEPLOY_SERVER_PATH=/srv/zeitschatz
